- Added an ACP lineage-v1 provenance producer: each agent session appends an append-only `lineage.jsonl` ledger beside its session artifacts (sessions, context epochs, compactions, requests), every provider turn and compaction summary call carries one opaque request ID on `X-ACP-Lineage-Request-ID` and `Idempotency-Key` (minted before the call, stable across retry attempts of the same call), spawned subagents record their parent session, depth, and spawning request, and `deriveLineageManifest` folds a session tree's ledgers into a manifest matching the verifiers lineage-v1 schema. Derivation only — nothing publishes or reads the ledger yet.
